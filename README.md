# fastapi-example
FastAPI Docker GitFlow example

# Clone the repository

```
git clone https://github.com/medranoq/fastapi-example.git

```

# Changes to workdir and starts services

```
docker compose up
```

# Make a request

`http://localhost` or use `http://0.0.0.0/`